The suffix of each HTML file is the ortholog family identifier.

Within the HTML file, full original MAFFT alignment is displayed.

The sites retained in final alignment are underlined by the blue blocks.